#!/usr/bin/env bash

###################################################################
# Description  : Run script for ansible execution
# Author       : DevSallSa
# Email        : sallyaxel@linuxmail.org
# Date         : 2022-12-24
# Version      : 1.0
###################################################################

sudo apt-get update -y
sudo apt install software-properties-common
sudo apt-add-repository --yes --update ppa:ansible/ansible
sudo apt install ansible

export XDG_CONFIG_HOME="$HOME"/.config
export XDG_DATA_HOME="$HOME"/.local/share

## pull ansible
#ansible-playbook -K --vault-pass-file "$HOME"/.vault-key \
#	--skip-tags=ssh,authorized-ssh-keys local.yml
#
#ansible-pull -K --vault-pass-file "$HOME"/.vault-key \
#	--skip-tags=ssh,authorized-ssh-keys -U https://github.com/DevSallSa/ansible.git

echo "RELOAD YOUR SYSTEM TO BE OK !!
